# Database Configuration
# These variables are used by the Flask backend application
# when running locally (outside of Docker).
#
# When running in Docker, these values are provided by docker-compose.yml
# from the root-level .env file or docker-compose environment section.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=
POSTGRES_PORT=
